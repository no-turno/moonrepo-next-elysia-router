type: application
language: typescript
platform: bun

toolchain:
  bun:
    version: "latest"

id: "web"

tags:
  - "web"
  - "nextjs"

dependsOn:
  - 'api'

tasks:
    dev:
      command: 'bun run dev'